# Core
Django==5.1.4
djangorestframework==3.15.2

# Auth
djangorestframework-simplejwt==5.3.1

# CORS — allow frontend (React/Next.js) to call the API
django-cors-headers==4.4.0

# Filtering — query params like ?chapter=laws_of_motion&weak_only=true
django-filter==24.3

# OpenAPI schema + Swagger UI
drf-spectacular==0.27.2

# LLM — OpenAI for Study Session chatbot
openai==1.54.4
httpx==0.27.2
anthropic==0.34.2


# Async tasks + scheduled jobs (Celery beat for daily/weekly analytics snapshots)
celery==5.4.0
redis==5.0.8
django-celery-beat==2.7.0

# Environment variables
python-dotenv==1.0.1

# Database — PostgreSQL for production (SQLite used in dev)
psycopg2-binary==2.9.10

# Utilities
Pillow==10.4.0        # avatar image uploads (ImageField)
